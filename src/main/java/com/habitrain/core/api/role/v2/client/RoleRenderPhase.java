package com.habitrain.core.api.role.v2.client;

/**
 * Where a role's identity is drawn (design §5.12 / P2).
 */
public enum RoleRenderPhase {
    NAMEPLATE,
    TAB,
    INTRO,
    SPECTATOR,
    INSANE
}
