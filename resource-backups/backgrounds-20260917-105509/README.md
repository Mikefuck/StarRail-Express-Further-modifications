# Background originals

The loading/ and title/ directories contain exact copies from before resizing.
initial-originals/ contains the initial files from before the earlier lossless compression.
These files are outside src/ and are not included by the current Gradle resource or JAR configuration.
manifest.json records original and resulting hashes, sizes and dimensions.
